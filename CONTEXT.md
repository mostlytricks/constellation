# CONTEXT — constellation

Last touched: 2026-07-11

## Completed
- Project scaffolded via `/init-project`; identity written into CLAUDE.md (the four-skill pipeline: ideate → analyze → templatize → compose).
- GitHub repo `mostlytricks/constellation` created (public) and first commit pushed.

## Current State
- Docs-only: no skills, no code, no venv yet.
- The four-skill pipeline is named but unbuilt; the deck-spec seam (analyze ↔ compose intermediate representation) is undefined.

## Next Step
- Decide the first skill to build — recommend **analyze** (read a real sample `.pptx` → component-pattern report), since it grounds the deck-spec schema in evidence instead of invention. Bring 1–2 real decks as fixtures.

---
